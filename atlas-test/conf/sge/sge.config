includeConfig '../shared.config'

process {
    executor = "crg" // replace with your sge executor
    queue = "pr-makarov-rocky9"
    scratch = false
    errorStrategy = 'retry'
    maxRetries = 3
}
